import { HighlightLogoDirective } from './highlight-logo.directive';

describe('HighlightLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLogoDirective();
    expect(directive).toBeTruthy();
  });
});
